// Logging is configured via tauri-plugin-log in lib.rs setup.
// This module is reserved for future custom log filtering / interceptors.

pub fn init() {
    // placeholder; actual configuration happens in tauri::Builder plugin registration.
}
